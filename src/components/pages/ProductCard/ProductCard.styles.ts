import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  card: {
    width: 250,
    height: 420,
    marginRight: 25,
    marginLeft: 25,
    marginBottom: 50,
  },
  cardContent: {
    padding: 0,
  },
});
